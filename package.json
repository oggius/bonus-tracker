{
  "name": "bonus-tracker",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "pnpm --filter @bonus-tracker/web test:e2e"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  },
  "dependencies": {
    "sonner": "^2.0.7"
  }
}